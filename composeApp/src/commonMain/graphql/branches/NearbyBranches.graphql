query NearbyBranches(
  $longitude: Float!
  $latitude: Float!
  $radiusKm: Float!
  $first: Int
  $after: String
  $jwt: String
) {
  nearbyBranches(
    longitude: $longitude
    latitude: $latitude
    radiusKm: $radiusKm
    first: $first
    after: $after
    jwt: $jwt
  ) {
    edges {
      cursor
      node {
        id
        businessId
        name
        address
        phone
        isActive
        useAppMessaging
        vehicles
        tipos
        distanceM
        distanceKm
        avatarUrl
        avatarUrlBaja
        avatarUrlAlta
        coverUrl
        coverUrlBaja
        coverUrlAlta
      }
    }
    pageInfo {
      hasNextPage
      hasPreviousPage
      startCursor
      endCursor
      totalCount
    }
  }
}
