query GetBranch($id: String!, $jwt: String) {
  branch(id: $id, jwt: $jwt) {
    id
    businessId
    name
    address
    phone
    schedule
    tipos
    facilities
    avatar
    coverImage
    status
    deliveryRadius
    coordinates {
      type
      coordinates
    }
    managerIds
    createdAt
  }
}
