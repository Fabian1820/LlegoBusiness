query GetProducts($branchId: String, $categoryId: String, $availableOnly: Boolean = false, $first: Int = 100) {
  products(branchId: $branchId, categoryId: $categoryId, availableOnly: $availableOnly, first: $first) {
    edges {
      node {
        id
        branchId
        name
        description
        weight
        price
        currency
        image
        availability
        categoryId
        variantListIds
        createdAt
        imageUrl
      }
    }
    pageInfo {
      hasNextPage
      endCursor
      totalCount
    }
  }
}
