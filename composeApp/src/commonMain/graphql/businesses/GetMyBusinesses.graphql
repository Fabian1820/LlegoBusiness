query GetMyBusinesses($jwt: String!) {
    getMyBusinesses(jwt: $jwt) {
        id
        name
        ownerId
        globalRating
        avatar
        description
        socialMedia
        tags
        isActive
        createdAt
        avatarUrl
    }
}
