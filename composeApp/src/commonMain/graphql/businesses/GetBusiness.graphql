query GetBusiness($id: String!, $jwt: String) {
  business(id: $id, jwt: $jwt) {
    id
    name
    description
    avatar
    coverImage
    tags
    globalRating
    isActive
    createdAt
    ownerId
  }
}
