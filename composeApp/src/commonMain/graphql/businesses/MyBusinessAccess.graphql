query MyBusinessAccess($jwt: String!) {
    myBusinessAccess(jwt: $jwt) {
        isActive
        isExpired
        expiresAt
        daysUntilExpiration
        business {
            ...BusinessAccessFields
        }
    }
}
