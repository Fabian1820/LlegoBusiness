query BusinessAccessByBusiness($businessId: String!, $jwt: String!) {
    businessAccessByBusiness(businessId: $businessId, jwt: $jwt) {
        id
        isActive
        isExpired
        expiresAt
        daysUntilExpiration
        grantedAt
        user {
            ...UserAccessFields
        }
        business {
            ...BusinessReferenceFields
        }
    }
}
