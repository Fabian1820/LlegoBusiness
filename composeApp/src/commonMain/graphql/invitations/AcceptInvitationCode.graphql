mutation AcceptInvitationCode($input: AcceptInvitationInput!, $jwt: String) {
    acceptInvitationCode(input: $input, jwt: $jwt) {
        id
        code
        invitationType
        status
        accessDurationDays
        accessExpiresAt
        isUsable
        accessStatus
        createdAt
        usedAt
        business {
            ...BusinessReferenceFields
        }
        branch {
            ...BranchReferenceFields
        }
        creator {
            ...UserReferenceFields
        }
        redeemer {
            ...UserReferenceFields
        }
    }
}
