query GetProductsByIds($ids: [String!], $first: Int = 100) {
  products(ids: $ids, first: $first) {
    edges {
      node {
        id
        branchId
        name
        description
        weight
        price
        currency
        image
        availability
        categoryId
        variantListIds
        createdAt
        imageUrl
      }
    }
    pageInfo {
      hasNextPage
      endCursor
      totalCount
    }
  }
}
