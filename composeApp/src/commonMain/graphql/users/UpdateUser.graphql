mutation UpdateUser($input: UpdateUserInput!, $jwt: String!) {
  updateUser(input: $input, jwt: $jwt) {
    id
    name
    email
    username
    phone
    role
    avatar
    businessIds
    branchIds
    wallet {
      local
      usd
    }
    walletStatus
    avatarUrl
  }
}
